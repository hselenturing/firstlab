/*
#include <stdio.h>

int main()
{
	int ch1, ch2;
	ch1 = getchar();
	ch2 = fgetc(stdin);
	
	putchar(ch1);
	fputc(ch2, stdout);
	return 0;
}
*/
/*
#include <stdio.h>

int main()
{
	int ch;
	
	while (1)
	{
		ch = getchar();
		if (ch == EOF)
			break;
		putchar(ch);
	}
	return 0;
}
*/

#include <stdio.h>

int index(chi)
{
	if()
	if()
}

int main()
{
	int ch;
	ch = getchar();
	index(&ch);
}